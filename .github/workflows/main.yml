name: Demo GitHub Actions Workflow

on:
  push:

jobs:
  Discover-GitHub-Actions-Workflows:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world."
      - run: echo "Helloa sd world."
      - run: sleep 100
      - run: echo "Hello after time."
